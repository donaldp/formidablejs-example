export default {

	# --------------------------------------------------------------------------
	# Authentication Defaults
	# --------------------------------------------------------------------------

	defaults: {
		protocol: 'api'
	}

	# --------------------------------------------------------------------------
	# Authentication Protocols
	# --------------------------------------------------------------------------

	protocols: {
		api: {
			provider: 'session'
		}
	}

	# --------------------------------------------------------------------------
	# Providers
	# --------------------------------------------------------------------------

	providers: {
		jwt: {
			driver: 'jwt'
			hidden: [ 'id', 'password' ]
			table: 'users'
		}

		session: {
			driver: 'session'
			table: 'users'
		}
	}
}
